const config = {
    desarrollo: {
        "SERVER": "http://localhost",
        "PORT": "3000"
    },
    produccion: {
        "SERVER": "http://app.node.es",
        "PORT": "80"
    }
};

module.exports = config;